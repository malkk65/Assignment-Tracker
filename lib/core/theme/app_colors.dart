import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF289898);
  static const Color primaryLight = Color(0xFF3BB5B5);
  static const Color primaryDark = Color(0xFF1E7272);

  // Background
  static const Color scaffold = Color(0xFFF9F9F9);
  static const Color card = Color(0xFFFFFFFF);
  static const Color inputFill = Color(0xFFF2F2F2);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF546E7A);
  static const Color textHint = Color(0xFF9CA3AF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status
  static const Color error = Color(0xFFC9784D);
  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFFA726);
  static const Color urgent = Color(0xFFE53935);

  // Priority
  static const Color highPriority = Color(0xFFE53935);
  static const Color mediumPriority = Color(0xFFFFA726);
  static const Color lowPriority = Color(0xFF43A047);

  // Border / Divider
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);
}
